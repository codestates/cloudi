import styled from 'styled-components';

const ModalContainer = styled.div`
  width: 150px;
  height: 150px;
`;

const Modal = () => {
  return <ModalContainer>MMMMM</ModalContainer>;
};

export default Modal;
